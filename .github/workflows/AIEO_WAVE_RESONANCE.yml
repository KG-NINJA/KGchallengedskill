name: AIEO Wave Resonance Tracker
# 🚫 Disabled workflows placeholder
disabled: true

# 目的：
# - 旧ワークフローを一時保管
# - 再有効化時は disabled を削除して運用再開可能

# on:
#   schedule:
#     - cron: '0 0 * * *'
#   workflow_dispatch:
# jobs:
#   placeholder:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "Disabled Autonomous Actions Archive"
on:
  workflow_run:
    workflows: ["AIEO Effect Analyzer"]
    types: [completed]

concurrency:
  group: aieo_pipeline
  cancel-in-progress: true

jobs:
  resonate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install pandas matplotlib

      - name: Run Resonance Tracker
        run: python scripts/aieo_composite.py  # ← 半角スペースに修正済み

      - name: Commit & Push resonance results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add aieo_effect_chart.png aieo_effect_log.csv
          git commit -m "🎵 Updated AIEO Resonance Effect" || echo "No changes"
          git push
