name: 🌀 AIEO Master Pipeline (Visibility → Effect → Resonance)

on:
  workflow_dispatch:  # 手動実行も可
  schedule:
    - cron: "0 13 * * *"  # 毎日22:00 JST（13:00 UTC）に自動実行

concurrency:
  group: aieo_master_pipeline
  cancel-in-progress: true

jobs:
  # ① Visibility Tracker
  visibility:
    name: 🌐 Visibility Pulse
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install pandas matplotlib requests

      - name: Run Visibility Tracker
        run: python scripts/aieo_visibility.py

      - name: Commit & Push visibility results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add visibility_log.csv visibility_chart.png
          git commit -m "🌐 Updated AIEO Visibility Pulse" || echo "No changes to commit"
          git push

  # ② Effect Analyzer（Visibility結果を受け取る）
  effect:
    name: 🎯 Effect Analyzer
    needs: visibility
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install pandas matplotlib

      - name: Run Effect Analyzer
        run: python scripts/aieo_effect_analyzer.py

      - name: Commit & Push effect results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add aieo_effect_chart.png aieo_effect_log.csv
          git commit -m "🎯 Updated AIEO Effect Analyzer" || echo "No changes to commit"
          git push

  # ③ Resonance Composite（最終連鎖）
  resonance:
    name: 🎵 Resonance Composite
    needs: effect
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install pandas matplotlib

      - name: Run Resonance Composite
        run: python scripts/aieo_composite.py

      - name: Commit & Push resonance results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add aieo_effect_chart.png aieo_effect_log.csv
          git commit -m "🎵 Updated AIEO Resonance Composite" || echo "No changes to commit"
          git push
