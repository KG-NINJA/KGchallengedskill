name: Archived - Legacy AIEO Effect Analyzer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archive status
        run: |
          echo "This legacy workflow is archived."
          echo "Use AIEO Effect Analyzer 📈 or the AIEO Master Pipeline instead."
