name: Archived - Legacy AIEO Monthly Verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archive status
        run: |
          echo "This duplicate legacy workflow is archived."
          echo "Use AIEO Wave Resonance Tracker for the active resonance chain."
