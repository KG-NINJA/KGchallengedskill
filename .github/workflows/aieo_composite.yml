name: Archived - Legacy AIEO Composite

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archive status
        run: |
          echo "This legacy workflow is archived."
          echo "Use the AIEO Master Pipeline for Effect and Resonance generation."
