name: 🧠 AIEO Composite Tracker

on:
  schedule:
    - cron: "30 0 * * *"   # 毎日 UTC 0:30（日本時間 9:30）
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run composite index generator
        run: |
          pip install pandas matplotlib
          python scripts/aieo_composite_tracker.py

      - name: Commit & Push composite index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add aieo_composite_chart.png aieo_composite_log.csv
          git commit -m "🧠 Updated AIEO Composite Index"
          git push
