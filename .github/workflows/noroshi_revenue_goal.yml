name: Noroshi Unified Pulse
# 🚫 Disabled workflows placeholder
disabled: true

# 目的：
# - 旧ワークフローを一時保管
# - 再有効化時は disabled を削除して運用再開可能

# on:
#   schedule:
#     - cron: '0 0 * * *'
#   workflow_dispatch:
# jobs:
#   placeholder:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "Disabled Autonomous Actions Archive"

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  pulse:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate unified NOROSHI signal
        run: |
          TIMESTAMP=$(date -u)
          {
            echo "Last signal: $TIMESTAMP"
            echo ""
            echo "🚀 **KG-NINJA | AI × Geospatial × Automation**"
            echo "Building autonomous AI pipelines and experimental prototypes."
            echo ""
            echo "📝 🔥 AutoKaggler — Rapid Kaggle pipeline generator"
            echo ""
            echo "🌌 **Mission:** Launching daily NOROSHI signals into the AI cosmos 🌌"
            echo ""
            echo "☕ [Buy Me a Coffee](https://www.buymeacoffee.com/kgninja)"
            echo "🌐 [Linktree - All Links](https://linktr.ee/kgkk)"
            echo "💼 [Lancers（ランサーズ）](https://www.lancers.jp/profile/KGKGKG)"
            echo "📺 [YouTube - FuwaCoco](https://www.youtube.com/@FuwaCoco)"
            echo ""
            echo "#NOROSHI #KGNINJA #AIPulsar"
            echo ""
          } > NOROSHI_SIGNAL.md

      - name: Pick random skill from KGchallengedskill
        run: |
          curl -s https://raw.githubusercontent.com/KG-NINJA/KGchallengedskill/main/README.md -o skills.md
          SKILL=$(grep -E '^[\-\*]|^[0-9]+\.' skills.md | shuf -n 1)
          echo "🎯 **Today's Featured Skill**" >> NOROSHI_SIGNAL.md
          echo "" >> NOROSHI_SIGNAL.md
          echo "$SKILL" >> NOROSHI_SIGNAL.md
          echo "" >> NOROSHI_SIGNAL.md

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add NOROSHI_SIGNAL.md
          git commit -m "🌐 Unified Noroshi pulse signal update"
          git push
